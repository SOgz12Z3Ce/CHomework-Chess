#ifndef SAVE_H_
#define SAVE_H_

#include <stddef.h> 
#include <stdlib.h> 
#include <stdbool.h> 

#include "piece.h" 


bool save(piece_ptr_t **state, size_t row_size, size_t col_size)
piece_ptr_t **load(size_t row_size, size_t col_size)

#endif/结束标识